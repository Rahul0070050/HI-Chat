import React from 'react'

import './style.scss'

function Conversation() {
    return (
        <div className='conversation-window' style={{height: `${window.innerHeight - 110}px`}}>
            Conversation
        </div>
    )
}

export default Conversation